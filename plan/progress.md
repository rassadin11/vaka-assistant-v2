# Прогресс реализации

Правила: отмечать пункт только после полной приёмки (тесты + e2e + gitleaks); в скобках — дата и ссылка на коммит/PR, если есть. Блокеры — в раздел внизу. Формулировки пунктов — короткие ярлыки, содержание всегда в plan/stage-N.md.

## Этап 0. Подготовка
- [x] 0.1 Документ требований (2026-07-09, plan/requirements.md)
- [x] 0.2 Репозиторий (monorepo, pre-commit, pytest) (2026-07-09, initial commit: uv, ruff, mypy strict, pytest, pre-commit hooks установлены, Dockerfile собирается)
- [x] 0.3 CI (2026-07-09, зелёный прогон: lint, test, gitleaks, docker build — github.com/rassadin11/vaka-assistant-v2, run 29022815257)
- [ ] 0.4 Окружения dev/prod
- [ ] 0.5 Регистрации (BotFather, OpenRouter, Google Cloud + заявка, домен, Tribute)
- [ ] 0.6 Privacy policy опубликована; черновик оферты

## Этап 1A. Локальная инфраструктура
- [x] 1.1 docker-compose dev (`make dev-up`) (2026-07-09, Codex + приёмка Claude; commit 68d0eb8)
- [x] 1.2 PgBouncer × asyncpg smoke-тест (2026-07-09, Codex + приёмка Claude; в CI, commit 357c310)
- [x] 1.3 Роли БД + хелпер SET LOCAL + RLS-тест (2026-07-09, core/db.py + init-roles.sql; RLS-шаблон уточнён в db-schema.md — fail-closed NULLIF; commit 357c310)
- [x] 1.4 Два Redis (2026-07-09, в составе 1.1: noeviction+AOF / allkeys-lru без персистентности, проверено config get)
- [x] 1.5 Infisical + machine identities (2026-07-09, идемпотентный bootstrap, 3 identity верифицированы, core/secrets.py; учётки вне репо; Codex + приёмка Claude; commit de161cc)
- [x] 1.6 Миграция v1 (13 таблиц по db-schema.md) (2026-07-09, Codex + opus-ревью против спеки + приёмка Claude; уточнения спеки: RLS-шаблон fail-closed, user_id NOT NULL; commit 7875092)
- [x] 1.7 Envelope-шифрование oauth_tokens (2026-07-09, core/crypto.py: AES-256-GCM, версионируемый KEK, key_version в AAD; Codex + приёмка Claude; commit 46d4360)
- [x] 1.8 Бэкапы + restore-to-dev.sh (2026-07-09, pgBackRest 2 шифрованных репо 7d/4w + WAL, restore в отдельный контейнер с маркерной проверкой PASS, RDB-снапшоты redis-queue, дамп Infisical; Codex + приёмка Claude; commit 3ab7d4f)

DoD 1A выполнен (2026-07-09): `make dev-up` с нуля до рабочего состояния; миграция v1 ролью migrator; RLS-тест и PgBouncer smoke зелёные (локально и в CI); restore-to-dev.sh восстанавливает бэкап с проверкой.

## Этап 1B. Боевой сервер (к закрытой бете)
- [x] 1.9 Сервер + hardening (2026-07-16, 1.9.1 аренда + 1.9.2/1.9.3 hardening через роль base 1.10.2, применено и проверено вживую на 31.76.15.130)
- [x] 1.10 Ansible (2026-07-16, роли base/docker/app + скелет; molecule converge+idempotence в CI; все три применены и идемпотентны вживую на 31.76.15.130; app — каркас, выкладка compose/секретов отложена до 1.11–1.12)
- [ ] 1.11 Caddy (1.11.1 compose.prod ПРИНЯТ 2026-07-16: топология fable + Codex, живая выкладка ролью app на 31.76.15.130, `config -q` OK на сервере, идемпотентность 0 changed, CI зелёный, коммиты 5b0ee70+bcf233b. **Смена домена Б2: vaka-assistant.ru** (владелец отрепоинтил A → 31.76.15.130, подтверждено 2026-07-16) — 1.11.3 закрыт. 1.11.2 Caddy ПРИНЯТ 2026-07-16 (a4a12a2): TLS Let's Encrypt живой на vaka-assistant.ru, редирект 308, маршруты→gateway (502 до 1.12), security-заголовки; caddy запущен на сервере. 1.11.4 webhook ПРИНЯТ 2026-07-16: ключи Б4 досеяны, webhook на @vAssistantv2testbot (беты; workbot остаётся каналом сводок), живой e2e владельцем — онбординг+approve+диалог с web_search и create_reminder, записи в tool_calls_log; закрыта и e2e-часть 1B-x4. Пункт 1.11 ЗАКРЫТ ЦЕЛИКОМ)
- [ ] 1.12 CI-деплой (GHCR → сервер) (1.12.1 ПРИНЯТ 2026-07-16, 64da04f: workflow Images собирает и пушит 3 образа — app/postgres/embeddings, gha-кэш, smoke-импорт; все три тянутся на VPS анонимно (public); 1.12.2 ПРИНЯТ в объёме механизма 2026-07-16 (43b69f3+3954f8e): entrypoint секретов вживую тянет 6 секретов из прод-Infisical, боевые пароли БД, identities PASS; досев API-ключей ждёт Б4 [владелец: положить перевыпущенные OPENROUTER_API_KEY/TELEGRAM_BOT_TOKEN/GROQ_API_KEY в C:/Users/Artem/.assistant/prod-secrets.env]; 1.12.3 ПРИНЯТ 2026-07-16 в объёме ручного прогона (9371705+ce04f02): полный стек 17 контейнеров live на VPS, миграции v1 применены, unit enabled, reboot-тест пройден; первый CI-прогон workflow ждёт секретов DEPLOY_HOST/DEPLOY_SSH_KEY от владельца; 1B-x3 закрыт (алерты Grafana на канал владельца); осталось 1.12.4 expand-contract [отложен до второй миграции — проверять не на чем])
- [ ] 1.13 Бэкапы на проде (S3 у другого провайдера)

> Декомпозиция 1B готова (2026-07-15, plan/stage-1.md, трек 1B): подзадачи 1.9.x–1.13.x + сквозные 1B-x, оценки (~4.5–6 дней работы Claude+Codex сверх календарного ожидания владельца), роллап-таблица, DoD. **Первый шаг — за владельцем: блокеры Б1 (хостер+152-ФЗ, обсуждение №3), Б2 (домен+privacy policy), Б3 (S3-провайдер), Б4 (перевыпуск ключей).** Пока Б1/Б2 не сняты, инфра-код можно писать «вслепую» по спеке, но финальная приёмка — только на реальном VPS. Софт (этапы 2–6) готов и протестирован; переход на V4 Flash подключён для тестовой работы (env-конфиг, дефолт модели в коде пока deepseek-chat).
>
> **Б1 РЕШЕНО (2026-07-15):** курс — всё за рубежом на закрытую бету (кандидат Hetzner, EU), 152-ФЗ localization отложена как долг миграции к публичному запуску, юр-заключение до паблика; причины/режим — обсуждение №3 мастер-плана. Разблокированы 1.9/1.10, топология без релея (не-РФ аккаунт OpenRouter + не-РФ IP сервера). За владельцем осталось: Б2 (домен+privacy), Б3 (S3), Б4 (ключи). Следующий шаг — конкретный хостер/регион и способ оплаты.
>
> **Хостер выбран (2026-07-15):** не Hetzner (не берёт крипту и придирчив к РФ), а **xorek.cloud** (RUB-биллинг) с ЦОД в **Финляндии**, SKU **FI-R9-16** — 8 vCPU / 16 ГБ / 240 ГБ / 1 IPv4, ~1399 ₽/мес. **Сервер выдан и провалидирован (2026-07-15): IP 31.76.15.130, Ubuntu 24.04.4 LTS, 8 vCPU / 15Gi / 237G / x86_64; SSH по ключу работает. Ключевое: с финского IP OpenRouter=200, Groq=401 — не заблокировано, релей НЕ нужен, Б1 подтверждён вживую. 1.9.1 (аренда) закрыт.** Оплата в рублях → развилка способа оплаты закрыта. Если упрёмся в память — апгрейд до FI-R9-32. Нюанс машины владельца: SSH к серверу идёт мимо MantaRay VPN статическим маршрутом (детали — память machine-and-workflow-quirks).
>
> **1.10.1 (скелет Ansible) — ПРИНЯТО (2026-07-15, коммит b5ffb03):** infra/ansible/ (inventory, group_vars, vault.yml.example, site.yml, роли-заглушки base/docker/app, Molecule docker/Ubuntu 24.04) + CI-job (yamllint/ansible-lint/syntax/molecule). Приёмка: локально ansible-lint (профиль production)/yamllint/syntax/gitleaks зелёные; в CI оба workflow (Ansible + основной CI) — success, molecule converge+idempotence прошёл. Решения владельца: A — тестирование через Molecule-в-CI (закрывает идемпотентность 1.10.5), B — секреты через Infisical, Ansible держит только bootstrap-vault (детализация в stage-1.md). Дальше — 1.10.2 (роль base: hardening + пользователь deploy), приёмка вживую против 31.76.15.130.
>
> **1.10.2 (роль base: hardening + deploy) — ПРИНЯТО (2026-07-16, коммиты ef43c37 + 45c3692):** роль base наполнена и применена вживую к 31.76.15.130. Задачи: apt-пакеты; пользователь `deploy` + SSH-ключ + NOPASSWD-sudo; sshd drop-in (`PasswordAuthentication no`, `PermitRootLogin prohibit-password`, `MaxAuthTries 4`); ufw (deny in / allow out, 22/80/443); fail2ban sshd-jail; unattended-upgrades (security-only, без reboot); sysctl (`somaxconn=1024`, `overcommit=1`); timezone Europe/Moscow. Container-несовместимые задачи (ufw/fail2ban/sysctl + handlers restart) гейтятся по `virtualization_type`. **Живая приёмка:** bootstrap под root (`-e ansible_user=root`, т.к. host-var `ansible_user=deploy` перебивает `-u`) — 13 changed; deploy заходит по ключу + `sudo -n` даёт root; root по ключу остаётся (fallback подтверждён); парольный вход отклонён; на сервере проверены ufw active (22/80/443 v4+v6), fail2ban sshd active, sysctl 1024/1, tz Europe/Moscow, эффективный sshd (`passwordauthentication no`/`permitrootlogin without-password`); **второй прогон под `deploy` через sudo — ok=19 changed=0** (идемпотентность вживую + целевая модель доступа CI/деплоя). Локально molecule (converge+idempotence+verify) и CI (Ansible + основной) зелёные, gitleaks чисто. Инвентарь уже на `ansible_user=deploy` (root-bootstrap — разовый override). **Тех-долг (не блокер):** заменить top-level `ansible_virtualization_type` на `ansible_facts['virtualization_type']` до ansible-core 2.24 (сейчас — только deprecation-warning). Этим закрыт и пункт 1.9 (hardening). Дальше — 1.10.3 (роль docker).
>
> **1.10.3 (роль docker: Docker Engine + compose) — ПРИНЯТО (2026-07-16, коммиты 739d0a4 + 21325e1):** роль docker наполнена и применена вживую к 31.76.15.130. Официальный apt-репозиторий Docker (GPG в `/etc/apt/keyrings`, `signed-by`), пять пакетов пиновыми версиями из group_vars (docker-ce/cli 27.5.1, compose-plugin 2.32.4; containerd.io/buildx без пина), `daemon.json` (ротация логов json-file 10m×3, `live-restore: true`), deploy в группе `docker`, старт/рестарт демона гейтятся по `virtualization_type` (как в base). **Приёмка Claude поймала реальный баг:** `get_url` использовал несуществующий параметр `src` вместо `url` — упал бы на живом сервере; исправлено. **Живая приёмка (под deploy + sudo):** первый прогон 6 changed; на сервере `docker 27.5.1` + `compose v2.32.4`, `systemctl is-active docker` = active, deploy запускает `docker ps` **без sudo** из свежей сессии (членство в группе), `docker info` → log-driver json-file + live-restore true; **второй прогон 0 changed** (идемпотентность вживую). Локально molecule (converge+idempotence+verify) и ansible-lint(production, 0 warn)/yamllint/syntax/gitleaks — зелёные. Тех-долг: `apt_repository` уйдёт в ansible-core 2.25 (перейти на `deb822_repository`). Дальше — 1.10.4 (роль app: каталоги, compose.prod, entrypoint Infisical) — частично упрётся в 1.11.1 (compose.prod) и домен (Б2).
>
> **Б2 — домен выбран (2026-07-16):** `vakachat.ru` (уже принадлежит владельцу; NS на Selectel, сейчас apex A → 51.38.239.149). Решение: репоинт apex A → **31.76.15.130** для беты. `.ru` — осознанный риск в русле Б1 (заметнее привязка к РФ), сменить перед публичным запуском при желании. **Действие владельца:** в DNS-панели Selectel сменить A-запись `@` (и опц. `www`) на 31.76.15.130. Caddy/webhook/TLS (Let's Encrypt) — на мне в 1.11; ufw уже пускает 80/443. Вторая половина Б2 — privacy policy (написать + опубликовать на `https://vakachat.ru/...`) — остаётся.
>
> **1.10.4 (роль app: каркас — каталоги + unit) — ПРИНЯТО (2026-07-16):** роль app наполнена в объёме domain/архитектурно-независимого каркаса и применена вживую к 31.76.15.130. Сделано: `/opt/assistant` (deploy:deploy 0755), `/etc/assistant` (root:deploy 0750), systemd-unit `assistant.service` (Type=oneshot, ExecStart=`docker compose -f /opt/assistant/compose.prod.yml up -d`, User=deploy) — установлен + daemon-reload, **НЕ enabled / НЕ started**. **Осознанно отложено:** выкладка compose.prod.yml → 1.11.1 (топология — fable), bootstrap.env (machine-identity Infisical) → 1.12.2 (прод-Infisical + ключи Б4), enable/start → 1.12.3. **Живая приёмка:** первый прогон 3 changed, handler Reload systemd выполнился; на сервере каталоги с нужными правами, unit виден systemd, `is-enabled=disabled` + `is-active=inactive`; второй прогон 0 changed. molecule converge+idempotence+verify зелёные локально, ansible-lint(production, 0 warn)/yamllint/syntax зелёные. На приёмке починено: (1) ansible-lint — `command: systemctl is-enabled` заменён на stat wants-симлинка; (2) handler daemon-reload гейтнут по `virtualization_type` (в контейнере нет systemd-шины); (3) verify не грузит group_vars — `app_dir` в проверках заменён литералом. **Этим 1.10 (Ansible) закрыт в объёме ролей** (1.10.5 идемпотентность подтверждена на всех трёх ролях). Дальше — 1.11.1 (compose.prod, fable) + репоинт домена (владелец).

## Этап 2. Гейтвей и очередь
- [x] 2.1 Gateway FastAPI (+фильтр приватных чатов) (2026-07-09, webhook+polling+set-webhook, healthz/metrics; Codex до лимита + Claude; commit dfcef17)
- [x] 2.2 Дедуп update_id (после enqueue) (2026-07-09, инвариант оформлен fault-injection-тестом)
- [x] 2.3 Redis Streams, envelope, 16 партиций (2026-07-09, core/envelope.py + core/queue.py, контракт pydantic v1)
- [x] 2.4 Per-user lock (2026-07-09, core/locks.py: NX PX 180s, Lua extend/release только владельцем; Codex + приёмка Claude; commit dfc7295)
- [x] 2.5 Redelivery + DLQ (2026-07-09, XAUTOCLAIM idle>210с, счётчик из XPENDING, DLQ после 3 доставок + уведомления; фикс Claude: reclaim обрабатывает все заклейменные записи; commit dfc7295)
- [x] 2.6 Приоритеты interactive/background (2026-07-09, non-blocking interactive → background block=2s, режим interactive-only; commit dfc7295)
- [x] 2.7 Rate limit per-user (2026-07-09, core/rate_limit.py: Lua token bucket 20/мин burst 5; одно предупреждение за окно fire-and-forget; уточнение 2.7 в stage-2.md — доставка через sender из 2.8; Codex + приёмка Claude; commit 84e3c23)
- [x] 2.8 Отправка ответов (лимиты Telegram) (2026-07-09, core/telegram_sender.py: ~30/с глобально + ~1/с на чат, retry по 429, нарезка 4096; подключено в gateway и worker; Codex + приёмка Claude; commit 84e3c23)
- [x] 2.9 Онбординг беты + welcome//help + таймзона (2026-07-09, worker/onboarding.py: статусы в воркере, админ-команды под service, кнопки городов + текстовый fallback, черновые тексты; попутно фикс 2.10 — обработка всего батча XREADGROUP (мультипартиционный дроп ловил e2e); Codex + приёмка Claude; commit ea44526)
- [x] 2.10 Каркас воркера (+typing, graceful shutdown) (2026-07-09, worker/: Processor+EchoProcessor, инжектируемые колбэки (Telegram — на 2.8), дедуп 2-я линия, reconnect backoff, trace_id-логи; e2e на живом Redis; Codex + приёмка Claude; commit dfc7295)
- [x] 2.11 Тесты контура (нагрузка, chaos, DLQ, деплой) (2026-07-09, tests/contour/: 4 сценария по плану; нагрузочный тест вскрыл дыру 2.4 — skip без ACK ломал per-user порядок; введён predecessor-guard + ожидание лока, механизм зафиксирован в stage-2.md; Codex + приёмка Claude с ужесточением скана до 128; commit 4cd909f)

DoD этапа 2 выполнен (2026-07-09): все тесты 2.11 пройдены на живом окружении (74 passed, включая контурные); порядок сообщений одного пользователя не нарушается при 4 воркерах — проверено нагрузочным тестом 100 msg/с.

## Этап 3. Агентное ядро
- [x] 3.1 LLMProvider + OpenRouterProvider (+allowlist) + Mock (2026-07-10, core/llm*.py: pydantic-контракты, data_collection deny + allow_fallbacks false + usage accounting в каждом запросе, cost Decimal, openai только в llm_openrouter; живой e2e-запрос с учётом стоимости; Codex + приёмка Claude; commit 71f1f77)
- [x] 3.2 Агентный цикл (+промежуточные сообщения) (2026-07-10, core/agent.py: лимиты 10 вызовов/120с/бюджет ₽, прогресс-сообщение; наследие этапа 2 закрыто — продление лока, DLQ по фактическим попыткам process(), TaskContext c users.id; тестовый инструмент get_current_time; живой e2e через очередь с DeepSeek; Codex + приёмка Claude; commit 4f09d55)
- [x] 3.3 Malformed tool calls (2026-07-10: спека дописана в stage-3.md перед кодом; MalformedToolCallError в диспетчере — неизвестное имя с перечнем доступных, битый/не-объект JSON, "" = "{}"; в цикле кумулятивный счётчик malformed-раундов, после 2 ретраев stop_reason="malformed" + фолбэк без лишнего вызова LLM; живой e2e — испорченный JSON, DeepSeek самоисправился; Codex + приёмка Claude; commits 536a059+1bd5f91)
- [x] 3.4 Контекст-менеджер + текст промпта + eval ≥18/20 (2026-07-10: спека дописана в stage-3.md перед кодом; core/tokens (tiktoken cl100k), core/context_manager (блоки A–F, бюджеты кодом, группировка ходов без висячих tool-сообщений), core/prompt (PROMPT_VERSION v1, ядро ~560 токенов из 1500), core/summarize; evals/ вне CI — живой eval 20 сценариев, две итерации промпта (устойчивость к инъекции в память, запрет имитации tool-вызовов текстом, «нет инструмента → честный отказ»), allowlist=novita (fp8) зафиксирован в плане, 20/20 дважды подряд; Codex + приёмка Claude; commits 94d60fd+8ee2520)
- [x] 3.5 Семафор + retry + fallback-модель (2026-07-10: спека дописана в stage-3.md перед кодом; core/llm_resilient.py — обёртка ResilientLLMProvider: Lua-семафор sem:openrouter (лимит 8, TTL 180с от утечек, слот не держится во время backoff-сна), retry ×3 с полным джиттером на 429/5xx, circuit breaker cb:openrouter:{model} (≥3 подряд → fallback на cooldown 300с, русский алерт админу с NX-дедупликацией); вшито в worker/__main__.py; живой e2e — ответ через обёртку, семафор освобождён; Codex + приёмка Claude; commits d66a3f7+e47ee91)
- [x] 3.6 Интерфейс роутера моделей (2026-07-10: спека дописана в stage-3.md перед кодом; core/model_router.py — RouteRequest/ModelRoute/ModelRouter + StaticModelRouter (v1 всегда deepseek-chat, множитель 1); вшивка в воркер: бюджет × множитель, guard совпадения модели с настройками; правила роутинга отложены до решения №7 (вопрос отправлен владельцу в Telegram); смоук инициализации воркера; Codex + приёмка Claude; commits 4103936+c3d4a8e)
- [x] 3.7 Персистентность диалога (2026-07-10: спека дописана в stage-3.md перед кодом; core/dialog_store.py — load/save messages+dialog_summaries строго через user_transaction (RLS), полная трасса с tool-сообщениями, tokens при записи, meta {prompt_version, stop_reason}; AgentProcessor собирает контекст A–F из загруженной истории (3.4 вшит в путь агента), fire-and-forget суммаризация с границей по id; app-пул в воркере; живой e2e — диалог с памятью между задачами («как зовут кота?» → из истории БД), попутно вживую сработала обработка malformed 3.3; Codex + приёмка Claude; commits 08f41b6+0d4c3cf)
- [x] 3.8 Учёт стоимости в usage (2026-07-10: спека дописана в stage-3.md перед кодом; core/usage_recorder.py — per-task обёртка, записи по мере успешных generate() (долг 3.2 «потеря стоимости при таймауте» закрыт); core/usage_store.py — вставка в usage через user_transaction; атрибуция: задачи → interactive, суммаризация → background; живой e2e — обе задачи диалога видны в usage с токенами и $; Codex + приёмка Claude; commits c288304+c09d3ea)
- [x] **DoD этапа 3 подтверждён** (2026-07-10): диалог с памятью end-to-end с тестовым инструментом (e2e 3.7/3.8 — «как зовут кота» из истории БД, get_current_time живьём); стоимость каждой задачи в БД (usage, e2e 3.8); обрыв воркера не теряет задачу (контурные chaos+poison тесты перегнаны на текущем коде — зелёные)

## Этап 4. Инструменты
- [x] 4.1 Скелет реестра и диспетчера (+идемпотентность §1.7) (2026-07-10: детализация в stage-4.md перед кодом (4ac0339); core/tools.py — ToolSpec/ToolRegistry: pydantic-валидация (2 ретрая), user_id модели отбрасывается, idem:{update_id}:{n}, daily_limit-счётчики, best-effort лог в tool_calls_log включая outbox-исполнение; core/registry_dispatcher.py — per-task адаптер, malformed-семантика 3.3 сохранена; механика 4.8 в объёме 4.1: pending-ключи + inline-кнопки + confirm/cancel-callback + outbox_actions + service-процессор с ретраями; TaskContext + update_id/plan; живой e2e — DeepSeek вызвал тестовый mutating_external, полный цикл до done в outbox и двух записей в tool_calls_log; Codex + приёмка Claude (правка: лог outbox-исполнения); commit bb069cf)
- [x] 4.2 Финансы (реестр §5) (2026-07-10: детализация в stage-4.md перед кодом (aa908e6); tools/finance.py — add_transaction/query_transactions/set_budget/get_budget_status: Decimal-квантование, границы дня/месяца в users.timezone, период ≤366 дней, усечение payload 100 строк, пороги бюджета 80/100% в payload, PNG-график (matplotlib Agg) через новый TelegramSender.send_photo fire-and-forget; миграция не нужна — таблицы из v1; живой e2e — «Потратил 500 на такси» → 500.00/expense/transport + агрегация follow-up, обе записи в tool_calls_log; Codex + приёмка Claude (правка: удержание ссылок на задачи отправки графика); commit 4db3866)
- [x] 4.3 Напоминания + шедулер (§6.1–6.3) (2026-07-10: детализация в stage-4.md перед кодом (899c3b0); tools/reminders.py — create/list/cancel_reminder: repeat→cron от локального времени пользователя, лимит 25 активных COUNT-ом в транзакции, daily_limit 30, remind_at в прошлом → retryable с локальным временем; worker/scheduler.py — service-роль, тик 60с, at-least-once в одной транзакции, FOR UPDATE OF s SKIP LOCKED, one-off→done / recurring→croniter в таймзоне пользователя; croniter+types-croniter; живой e2e — «напомни завтра в 9 утра купить хлеб» → 06:00 UTC, тик доставил и пометил done; Codex + приёмка Claude (правка: FOR UPDATE OF s — не держать лок users во время отправки); commit 40f9f1a)
- [x] 4.4 Память + бенчмарк эмбеддингов + сервис (§7) (2026-07-10: детализация в stage-4.md перед кодом (5f13411); бенчмарк evals/embeddings_benchmark.py на 28 русских парах — победитель multilingual-e5-large (recall@5 0.929 vs 0.893 у bge-m3), обсуждение №4 закрыто, порог автоинжекта перекалиброван 0.55→0.80 правкой реестра §7.2 (a816eb0); embeddings/ — FastAPI-сервис (e5-префиксы, батч ≤64, compose-профиль ml); core/embeddings.py — протокол+HTTP-клиент+Mock; tools/memory.py — remember_fact: дедуп >0.92, вытеснение старейших сверх 500; автоинжект блока D top-5 ≥0.80 с обновлением last_used_at, деградация без падения задачи; живой e2e — факт сохранён, дедуп, совет про десерты сам учёл аллергию из памяти; Codex + приёмка Claude (правки: направление вытеснения было перевёрнуто — удалялись новые факты; изоляция venv в compose; CI-фикс — профиль ml); commits 5991db4+d407895)
- [x] 4.5 SearXNG + web_search/fetch_page (§8) (2026-07-10: детализация в stage-4.md перед кодом (3c444ba); SearXNG в compose (дефолтный профиль, 127.0.0.1:8091, json, limiter off); core/ssrf.py — validate_public_url: каждый резолвленный адрес обязан быть глобальным, инжектируемый резолвер, SSRF-матрица тестов; tools/web.py — web_search (кэш redis-cache по нормализованному запросу TTL 1ч, недоступность → not retryable) и fetch_page (ручные редиректы ≤5 с ревалидацией хопов, обрыв 5 МиБ, text/* only, trafilatura, 20k символов); живой e2e — web_search через реальный SearXNG с DeepSeek + кэш; SSRF-фильтр вживую заблокировал VPN fake-IP 198.18.x.x (квирк машины), happy-path fetch_page проверен с DoH-резолвом (example.com ok); Codex + приёмка Claude (правка: getaddrinfo в asyncio.to_thread — не блокировать event loop); commit b95289f)
- [x] 4.6 PDF-пайплайн + search_documents (§9) (2026-07-11: детализация в stage-4.md перед кодом (81de7a8); gateway маршрутизирует kind=document в background-очередь; TelegramSender.download_file с капом 20 МиБ; worker/documents.py — PdfIngestProcessor: PyMuPDF постранично, OCR-фолбэк при <50 симв/стр (растеризация PyMuPDF + pytesseract rus+eng, TESSERACT_CMD, лимит 100 стр), чанкинг 800/100 токенов в границах страницы, эмбеддинги passage-батчами ≤64, месячный счётчик doc_pages 200 стр/мес с TTL; tools/documents.py — search_documents (top-6 cosine, документ+страница в payload), list_documents, delete_document (каскад); правка реестра §9.1 — pdf2image/poppler заменены растеризацией PyMuPDF; живой e2e — текстовый PDF и скан оба ready, OCR распознал скан вживую, DeepSeek через search_documents ответил со ссылкой на страницу, обе записи в tool_calls_log, русский OCR проверен отдельно (tesseract 5.4 + rus.traineddata, D:\tessdata); Codex + приёмка Claude (правки: парсинг/OCR в asyncio.to_thread — не блокировать event loop; pytesseract требует PIL Image, а не BytesIO — OCR-ветка была нерабочей; pymupdf/pytesseract в основные зависимости — CI ставит только --dev; EXPIRE на месячный счётчик; вычисляемый месяц в тестах); commit b055085)
- [ ] 4.7 Google OAuth + calendar/gmail (§10) — ждёт №5 и верификацию; указание владельца (Telegram, 2026-07-10): всё связанное с Google пока пропускать
- [ ] 4.8 Подтверждения mutating_external + outbox
- [x] 4.9 Голосовые (STT, §11) (2026-07-11: №6 решён владельцем — Groq whisper-large-v3, зафиксировано в мастер-плане; детализация в stage-4.md перед кодом (b9401d0); core/stt.py — STTProvider/GroqSTTProvider (verbose_json — обычный json не отдаёт duration, поймано на приёмке против живого API; cost из GROQ_STT_USD_PER_MINUTE, дефолт 0.00185)/Mock; worker/voice.py — VoiceProcessor: >5 мин отказ до скачивания, лимит 10 мин/день (stt_min:{user}:{день} UTC, TTL 2 суток, INCRBY после успеха), транскрипт → text-envelope с meta {modality: voice, duration}; usage model='stt:groq' (токены NULL, interactive); KindRouter (document/voice); ключ владельца добавлен и проверен; живой e2e — русская речь (TTS WAV, ffmpeg на машине нет — формат не влияет, провайдер принимает байты) → Groq распознал дословно → DeepSeek создал напоминание из сказанного, отказ 301с, лимит, usage-строка $0.00033, meta в messages; Codex + приёмка Claude (правка: verbose_json); commit e0a0b89)
- [x] /feedback + финальные welcome//help (2026-07-11: детализация в stage-4.md перед кодом (1fac294); /feedback <текст> от active-пользователя → пересылка админам «Отзыв от {tg_user_id}: …» (усечение 1000) + подтверждение всегда (сбой доставки — только в лог); пустой /feedback → подсказка; /feedbackfoo не перехватывается; финальные тексты welcome//help — бюджеты/напоминания/поиск/PDF/память, строка про Google убрана до 4.7; живой e2e — отзыв доставлен в реальный админ-чат; Codex + приёмка Claude без правок; commit 2032cd6)
- [ ] ВЕХА: закрытая бета

## Этап 5. Фоновые задачи и лимиты
- [x] 5.1 schedule_task (§6.4–6.6) (2026-07-11: детализация в stage-5.md перед кодом (38d42be); tools/scheduled.py — schedule_task (croniter, мин. интервал 1ч по 5 срабатываниям, лимит 10 активных в транзакции, daily_limit 10), list/cancel_scheduled_tasks; EnvelopeKind + "agent_task" (создаёт только шедулер); worker/scheduler.py — due agent_task → enqueue в background с детерминированным update_id sha256(task_id, next_run_at) (дедуп при переигровке), ошибка enqueue откатывает транзакцию; AgentProcessor принимает agent_task (промпт из payload, префикс «⏰ {title}», usage → background); живой e2e — DeepSeek создал задачу «каждый день в 9 утра сводка расходов» (cron 0 9 * * *, 06:00 UTC), принудительный тик поставил в реальный стрим, запуск прошёл агентный путь с query_transactions и честным ответом, next_run_at продвинут; Codex + приёмка Claude без правок; commit 2e294d0)
- [x] 5.2 Дневные бюджеты ₽ + деградация (2026-07-11: детализация в stage-5.md перед кодом (432dd5a); core/spend.py — ключ spend_rub:{user}:{YYYYMMDD} в таймзоне пользователя, TTL 3 суток, курс USD_RUB_RATE (дефолт 90), маппинг бюджета по плану (trial/standard 15₽, pro 45₽, env-переопределение), ступени ≥100% no_background / ≥120% short_context / ≥150% soft_refuse, fail-open при недоступном Redis; AgentProcessor — состояние читается раз в начале задачи, agent_task при ≥100% пропускается с одноразовым (NX EX до полуночи пользователя) уведомлением, lean-контекст (без блока D, хвост вдвое) при ≥120%, мягкий отказ без вызова LLM при ≥150%, инкремент трат по факту записи usage (задачи + суммаризация); VoiceProcessor — отказ до скачивания и STT, STT-стоимость в счётчик только при записанной usage-строке; лог ступени деградации для наблюдаемости; живой e2e сабагентом — все три ступени вживую (уведомление ровно одно, DeepSeek ответил на lean-контексте с инкрементом 18→18.054₽, отказ текст+голос без обращений к OpenRouter/Groq, после удаления ключа нормальная работа, TTL ≈259200); Codex (реализация из прошлой сессии в дереве) + приёмка Claude (правки: фикс assert в тесте голоса, лог деградации); commit 8672238, CI зелёный)
- [x] 5.3 Счётчики тарифных лимитов (2026-07-11: детализация в stage-5.md перед кодом (6a87758); core/limits.py — счётчик msg:{user}:{YYYYMMDD} в таймзоне пользователя (TTL 3 суток), лимит по плану trial/standard 100 / pro 200 + env DAILY_MESSAGE_LIMIT_DEFAULT, fail-open; limits_snapshot по четырём осям (сообщения, бюджет ₽ 5.2, минуты голоса 4.9, страницы PDF 4.6 — реальные форматы ключей) как задел для 5.4; core/time_keys.py — общий хелпер локальной даты (spend_key переиспользует); AgentProcessor — проверка лимита сообщений до бюджетной 5.2, инкремент только принятых text перед агентным циклом (отказы и agent_task не считаются); VoiceProcessor — отбивка до скачивания и STT; живой e2e сабагентом — при счётчике 100 текст и голос отбиты точным текстом без обращений к OpenRouter/Groq и без инкремента, после DEL нормальный ответ, счётчик 1 с TTL ≈259200, spend-инкремент 5.2 не сломан; Codex + приёмка Claude без правок; commit f7feaf9, CI зелёный)
- [x] 5.4 Уведомления 80% (2026-07-11: детализация в stage-5.md перед кодом (51673ee); core/limits.py — LimitAxis + claim_limit_notice (SET NX limit_notice:{axis}:{user}:{period}, датированный период по формату ключа оси, TTL 3/40 суток, fail-open); AgentProcessor — fire-and-forget после завершения задачи: ось бюджета для text+agent_task, ось сообщений только для text, значения после инкрементов текущей задачи; VoiceProcessor — предупреждение после инкремента минут (UTC-день, опциональный send-колбэк из entrypoint); PdfIngestProcessor — месячное предупреждение дописывается к успешному ответу; попутно фикс протухшей даты в test_reminders (2027); живой e2e сабагентом — msg=80+spend=12₽ → основной ответ + оба предупреждения с точными текстами (used=81 после инкремента), повтор без дублей (NX), голосовая ось прямым вызовом VoiceProcessor на реальном Redis (8/10 мин → предупреждение, повтор молчит), ниже порога — ноль предупреждений; Codex + приёмка Claude без правок; commit 0e48643, CI зелёный)
- [x] **DoD этапа 5 подтверждён** (2026-07-11): искусственное превышение бюджета вызывает деградацию по ступеням (e2e 5.2 — все три ступени вживую); счётчики сбрасываются корректно в таймзонах пользователей (ключи по локальной дате — e2e 5.2/5.3: смена ключа = смена дня, юниты на полночь пользователя ≠ UTC)

## Этап 6. Наблюдаемость
- [x] 6.1 Structured logging + trace_id (2026-07-12: детализация в stage-6.md перед кодом (5b4aba2); core/logging_setup.py — setup_logging(service), JSON-дефолт (ts ISO UTC, level, logger, service, trace_id, message + extra-поля, exc_info строкой), LOG_FORMAT=text сохраняет старый формат, LOG_LEVEL; core/tracing.py — ContextVar trace_id, приоритет extra > ContextVar > "-"; вшивка: gateway вокруг обработки update (uuid == envelope.trace_id), worker на каждый envelope, шедулер на каждую due-строку (trace уходит в agent_task-конверт), outbox на каждую строку; _TraceIdFilter удалён; живой e2e сабагентом — все строки лога валидный JSON, 7 строк задачи с единым trace_id == trace_id конверта (включая httpx и agent_processor — contextvar наследуется), text-режим соответствует старому формату; Codex + приёмка Claude без правок; commit 59314e5, CI зелёный)
- [x] 6.2 Prometheus + Grafana (+продуктовые метрики) (2026-07-12: детализация в stage-6.md перед кодом (67a81b5), правка db-schema.md — роль metrics_ro, кэш-хит prompt caching исключён из скоупа (кэширование не заложено); prometheus-client; core/metrics.py — счётчики gateway (received/enqueued/dedup/rate-limited), task_duration_seconds histogram, tasks_processed по исходам, queue_depth gauge (сборщик 15с по всем 32 стримам), llm_requests/fallback, llm_cost_rub_total по атрибуции, stt/tool_calls; gateway /metrics — реальная экспозиция вместо заглушки 2.1; воркер — WORKER_METRICS_PORT 9100 (0/pytest = off); infra — compose prometheus+grafana (localhost, volumes), scrape-конфиг, Grafana provisioning: datasources Prometheus+PostgreSQL (metrics_ro через pgbouncer) и два дашборда («Технический» PromQL, «Продуктовый» plain SQL по messages/usage/tool_calls_log); init-roles.sql — идемпотентная metrics_ro (BYPASSRLS, SELECT-only) + userlist pgbouncer; живой e2e сабагентом — оба таргета UP, метрики ненулевые после живого прогона (cost 0.0569₽), оба дашборда в Grafana API, продуктовые SQL возвращают данные (DAU/WAU, tool-распределение) под metrics_ro; нюанс: работающему pgbouncer нужен RELOAD для перечитки userlist (на свежем dev-up неактуально); Codex + приёмка Claude без правок; commit a105505, CI зелёный)
- [ ] 6.3 Sentry — БЛОКЕР за владельцем (2026-07-12: нужен выбор — облачный sentry.io (аккаунт заводит владелец, данные об ошибках в их облако) или self-hosted/отложить; вопрос отправлен в Telegram, msg 39; пропускаю к 6.4)
- [x] 6.4 Алерты (2026-07-12: детализация в stage-6.md перед кодом (4d10d71) — Grafana Unified Alerting + blackbox_exporter, без Alertmanager; Codex-реализация 17985be (infra/blackbox/, scrape-job blackbox, provisioning alerting/{rules,contact-points,policies}.yml, env-прокидка TELEGRAM_ALERT_*, README) + правки приёмки Claude: явный --config.file у blackbox, уникальный title EmbeddingsProbeFailed, и главный фикс d8850c3 — правила `up == 0`/`probe_success == 0` возвращали значение 0 и Grafana считала их Normal навсегда, заменено на `== bool 0` (поймано живым e2e: probe 10 минут в нуле при inactive-правиле); noDataState→OK для продуктовых правил (нет трафика ≠ инцидент, недоступность закрывает TargetDown, иначе DatasourceNoData-спам). Живой e2e: blackbox searxng/postgres probe_success=1 (embeddings=0 — профиль ml выключен, допустимо по плану); стоп searxng → ServiceProbeFailed Pending→Firing (попутно живьём загорелись EmbeddingsProbeFailed и TargetDown по незапущенным на хосте gateway/worker — правила работают), доставка в Telegram владельца подтверждена nflog алертменеджера (запись telegram-admin появляется только после успешной отправки), ошибок отправки 0; старт searxng → Normal за 30с, resolved-уведомление тоже в nflog (86→127 байт); тест contact point через provisioning-API невозможен — эндпоинт удалён в текущей Grafana (410), новый k8s-эндпоинт не принял ни один формат, заменён более сильным доказательством nflog; после e2e grafana пересоздана без токена (dev-дефолт по плану — иначе TargetDown по выключенным gateway/worker слал бы владельцу повторы каждые 4ч); gitleaks чистый (113 коммитов); Codex + приёмка Claude; commits 17985be+d8850c3, CI зелёный run 29203969175)
- [x] 6.5 Runbook (2026-07-12: детализация в stage-6.md перед документом; docs/runbook.md — 7 разделов: где смотреть (Grafana/Prometheus/логи/trace_id), падение воркеров (TargetDown, at-least-once), переполнение очереди (XLEN/XPENDING по партициям, разбор DLQ q:dlq), недоступность LLM (breaker/семафор/проверка ключа OpenRouter), недоступность сервисов (мягкая деградация searxng/embeddings, критичность postgres), восстановление из бэкапа (pgBackRest, make backup*/restore-to-dev, ловушка --profile restore down), сбор контекста по trace_id; команды прогнаны вживую на dev-стеке (XLEN/XPENDING/XRANGE/KEYS/SCARD, backup-check, проверка ключа OpenRouter), prod-места помечены [prod: 1B]; ссылка из infra/README.md; документ писал Claude (текст, делегирование дороже); gitleaks чистый; commit 1c4b1a7, CI зелёный run 29204482520)
- [x] 6.6 Правило проверки бэкапа (2026-07-14: календарное правило оформлено scheduled task `monthly-backup-restore-check` на машине владельца — 1-го числа каждого месяца в 12:00 МСК: make backup-check + make restore-to-dev (успех = PASS) + отчёт владельцу в Telegram; задача read-only (без правок файлов — урок автозапуска 03:00); нюансы: выполняется при открытом приложении Claude (иначе при следующем запуске), владельцу стоит один раз нажать Run now для преодобрения инструментов; процедура описана в runbook §5)
- [~] 6.7 Критерии выхода из беты — черновик чисел записан в stage-6.md (2026-07-14: D7 ≥30%, себестоимость ≤140₽/мес и ≤0.5₽/сообщение, 0 потерь данных + DLQ <0.5% за 14 дней, чек-лист обвязки, выборка ≥20 пользователей), отправлен владельцу в Telegram на подтверждение; дедлайн финализации — до конца беты

## Этап 7. Онбординг и биллинг
- [ ] 7.1 Tribute webhook — ждёт №8
- [ ] 7.2 Триал (7 дней ИЛИ 50 сообщ.)
- [ ] 7.3 Grace-период
- [ ] 7.4 Тариф v1 — финал лимитов по №11
- [ ] 7.5 GDPR экспорт/удаление — ждёт №9
- [ ] 7.6 Публичная оферта
- [ ] 7.7 Отключение ручного одобрения
- [ ] ВЕХА: публичный запуск

## Этап 9. Mini App: календарь + финансы (план создан 2026-07-17: plan/stage-9.md + спеки miniapp-calendar-spec.md / miniapp-finance-spec.md; источник — бета-фидбек владельца 2026-07-17 п.4 + решение расширить финансы до дашборда)
- [ ] 9.1 Платформа Mini App (webapp-сервис, initData-auth, фронт-каркас, Caddy /app/*)
- [ ] 9.2 Экран «Календарь» (по miniapp-calendar-spec.md)
- [ ] 9.3 Экран «Финансы» (по miniapp-finance-spec.md)
- [ ] 9.4 Интеграция с ботом (кнопка меню, inline-кнопки, /help)

## Внеплановые задачи
- [x] Доработки по бета-фидбеку владельца 2026-07-17 (ПРИНЯТО и ЗАДЕПЛОЕНО в тот же день, commit b9151e2, образ sha-b9151e2… на проде, стек healthy, воркеры model=deepseek/deepseek-v4-flash prompt=v2-flash, tesseract rus+eng в образе подтверждён; реализация Codex 4 батча, ревью Opus 4.8 — 1 should-fix исправлен (фото без file_size), приёмка Claude: ruff/mypy/pytest 284 passed, gitleaks чистый (14 находок — только .venv), eval v2-flash 33/32/32 из 33 ×3, v1 31/33 при пороге 30; поведенческий e2e фото/транзакций/markdown — за владельцем в живом боте) — детализация в docs/bot-analysis-2026-07-17.md (одобрена владельцем; п.4 календарь ИСКЛЮЧЁН — будет отдельным мастер-планом). Состав: (а) быстрые фиксы — direction-фильтр today_total в add_transaction, warning+метрика на игнорируемые апдейты, depends_on infisical healthy в compose.prod; (б) промпт v1+v2-flash — авто-запись транзакций без переспроса, привязка к текущей дате, identity без выдумок про GPT + eval-сценарии, порог 90%; (в) обратная связь при сбоях — одноразовое уведомление о долгой обработке/ретрае, заглушка на неподдержанные типы сообщений из gateway; (г) фото/OCR — tesseract (rus+eng) в прод-образ, EnvelopeKind расширяется значением "photo" (контрактная правка, зафиксирована здесь), photo+caption в envelope, worker-ветка OCR фото → текст в агентный цикл, caption у document; (д) markdown-лайт → Telegram HTML в core/telegram_format.py + parse_mode="HTML" в TelegramSender с фолбэком на plain text (замечание владельца от 2026-07-17, вторая итерация). Переключение прода на V4 Flash выполнено 2026-07-17 (Infisical OPENROUTER_MODEL/OPENROUTER_ALLOWED_PROVIDERS, лог воркера подтверждает v2-flash).
- [x] Промпт-итерация под V4 Flash (2026-07-15, ПРИЁМКА ПРОЙДЕНА; детализация и результат — pricing-draft.md §2a): model-specific промпт `v2-flash` (core/prompt.py — реестр PROMPTS/get_prompt, build_context(prompt_version=…), v1 байт-в-байт как раньше), eval расширен до 30 сценариев (+5 unknown-*, +5 pending-*, build_scenarios, EVAL_PROMPT_VERSION, порог ≥27/30), негейт-осознанный чекер _no_tool. Результат: V4 Flash+v2-flash — 30/30 три раза подряд (Parasail fp8, temp 0); V3-контроль (deepseek-chat, v1) на том же наборе — 30/30 без регрессии. 4 адресных итерации промпта (честный отказ без нарратива «сейчас посмотрю», запрет перевызова в pending, жёсткое правило «нет инструмента для сумм → не выдумывать»); флап на unknown-transactions с фабрикацией фейковой финсводки стабилизирован 4-й итерацией. Полная приёмка: ruff/mypy/pytest (274 passed) + gitleaks (no leaks) + CI. **Переключение базовой модели — отдельное решение владельца** (архитектурно бесплатно: OPENROUTER_MODEL/allowlist конфигом, StaticModelRouter; экономия ~2.2×, маржа Базового ~36%→~61%; перед переключением проверить стабильный fp8-хостер — на прогоне Novita был rate-limited, шли через Parasail). Вопрос владельцу отправлен в Telegram.

## Отложенные обсуждения (статус — мастер-план)
- [ ] №10 Google-заявка (этап 0) | [ ] №3 хостер+152-ФЗ | [ ] №5 OAuth | [ ] №6 STT | [ ] №7 роутинг (набросок к 3.6) | [ ] №8 Tribute | [ ] №9 хранение | [~] №11 лимиты (черновик готов, финал перед 7.4)

## Ревизии плана (регламент п.5)
- [x] После этапа 2 (2026-07-10: контракты 2↔3 стыкуются без изменений; в stage-3.md 3.2 внесены обязательные наследия этапа 2 — продление лока в цикле, DLQ-порог по фактическим попыткам process(), прокидывание users.id из onboarding-резолва; сроки и состав этапов 3+ без правок; блокеры 0.5/0.6/№10 достигли дедлайна — требуют действий владельца, см. Блокеры)
- [x] После этапа 4 (2026-07-11, проведена после закрытия этапа 5 — 4.7 отложен указанием владельца и этап 4 формально открыт; выводы: (1) контракты 4↔5↔6 стыкуются без правок мастер-плана, вскрывшиеся решения уже внесены ранее (₽-бюджеты, Groq, novita, e5); (2) 4.8 реализован в составе 4.1 целиком, остаётся только живая проверка на реальном внешнем инструменте — блокируется 4.7; (3) темп сильно выше планового (этапы 3–5 за ~2 дня против ~9 плановых) — сроки не правим, запас; (4) критическая зависимость сместилась с кода на владельца: 1B (боевой сервер) блокируется №3 (хостер/152-ФЗ), веха закрытой беты — 1B + 0.4/0.5/0.6, 4.7 — домен→privacy→заявка Google (№10, дедлайн давно наступил); (5) оставшаяся автономная работа — этап 6 (наблюдаемость, выполнима в dev), опционально прогон eval V4 Flash из pricing-draft §2a для решения по базовой модели; (6) актуальность отложенных обсуждений без изменений: №7 к 8.4, №8/№9 к этапу 7, №11 финал перед 7.4)

## Передача сессии (2026-07-12) — закрыта 2026-07-12 вечером: Docker ожил после рестарта Desktop, приёмка 6.4 доведена до конца (см. отметку 6.4 выше; блокер Docker снят, но диск C: по-прежнему 96% — следить). Следующий пункт — 6.5 Runbook (детализации в stage-6.md нет — сначала дописать). Исходный текст передачи ниже.

- **6.4 Алерты — реализовано, приёмка НЕ завершена** (коммит 17985be, WIP, не отмечать выше до полной приёмки). Сделано: детализация в stage-6.md (4d10d71 — Grafana Unified Alerting + blackbox_exporter, без Alertmanager); Codex-реализация (infra/blackbox/, scrape-job blackbox, infra/grafana/provisioning/alerting/{rules,contact-points,policies}.yml, env-прокидка TELEGRAM_ALERT_BOT_TOKEN/CHAT_ID в grafana, README); две правки приёмки Claude: явный `command: --config.file=/config/blackbox.yml` (иначе образ читает дефолтный конфиг) и уникальный title `EmbeddingsProbeFailed` (Grafana требует уникальность в папке). Пройдено: дифф целиком, ruff/mypy, pytest (269 passed; 2 интеграционных падали в полном прогоне от интерференции нагрузочного теста — по одному зелёные), `docker compose config -q`. **Осталось**: gitleaks → живой e2e (grafana+prometheus+blackbox перезапустить с новыми конфигами, экспортировать TELEGRAM_ALERT_BOT_TOKEN/CHAT_ID перед dev-up, стоп searxng → Firing → Telegram-уведомление → resolved) → пуш → зелёный CI → отметка 6.4 → dev-log → переиндексация codebase-memory → сводка владельцу.
- **БЛОКЕР среды: Docker Desktop деградировал** — новые контейнеры застревают в статусе Created (не стартуют): gitleaks, alpine, любые `docker run`; давно запущенные (dev-стек) работают, `docker pull` работает. Из-за этого gitleaks не прогнан. Лечение вероятно рестартом Docker Desktop (не делал — уходил по стопу владельца); после рестарта поднять dev-стек `make dev-up`. Зависшие Created-контейнеры удалить: `docker ps -a` → `docker rm`. Возможная первопричина — хронически переполненный диск C:.
- 6.3 Sentry — блокер за владельцем (вопрос в Telegram без ответа). Сообщение владельца «остановись пока что» (2026-07-12 00:33 МСК) прочитано, offset сдвинут (399520589); текущая работа велась по явному указанию в чате «продолжай».
- Следующая сессия: сначала починить Docker → добить приёмку 6.4 (список выше) → чекпоинт Telegram → 6.5 Runbook (детализации в stage-6.md ещё нет — сначала дописать).

## Передача сессии (2026-07-11, вечер) — остановка по указанию владельца (лимиты)

- Приняты за 2026-07-11: **4.6** (PDF-пайплайн, b055085), **/feedback + финальные welcome//help** (2032cd6), **5.1 schedule_task** (2e294d0), **4.9 голосовые через Groq** (e0a0b89), **5.2 дневные бюджеты ₽ + деградация** (8672238) — все с зелёным CI, живыми e2e и dev-log. №6 решён владельцем (Groq), ключ добавлен и проверен.
- Примечание к 5.2: реализация Codex оказалась в рабочем дереве уже при возобновлении (вопреки записи «дерево чистое») — принята после полной проверки: дифф соответствует детализации, найден и исправлен один битый assert в тесте.
- **Следующий пункт — 5.3** (счётчики тарифных лимитов): детализация в stage-5.md ещё не написана — сначала дописать её (числа из pricing-draft.md, черновик №11), потом Codex. После — 5.4 (уведомления 80%). Этап 4: остались 4.7 (Google — пропускать по указанию владельца) и веха беты (за владельцем). Ревизия плана после этапа 4 ещё не проводилась (4.7 открыт).
- Квирки машины и регламент приёмки — в памяти Claude и разделе ниже; tesseract и tessdata на месте (D:\tessdata), embeddings-сервис останавливается docker compose --profile ml stop embeddings при необходимости.

## Передача сессии (2026-07-10, ночь)

- Этап 2 закрыт целиком; ревизия после этапа 2 выполнена; **Этап 3 закрыт целиком (3.1–3.8 + DoD, 2026-07-10).** Дальше: ревизия плана после этапа 3 не предусмотрена регламентом (следующая — после этапа 4), следующий пункт — **4.1 (реестр инструментов: ToolDefinition, диспетчер с валидацией по JSON-схеме, tool_calls_log)** по plan/stage-4.md + tools_registry_v1.md разделы 1–3. Внимание: блокеры домен/№3/privacy/Google (критический путь к 4.7) всё ещё за владельцем — вопрос отправлен в Telegram. Вопросы владельцу (№7 роутинг, блокеры домен/хостер/privacy/Google) отправлены в Telegram-канал 2026-07-10. Канал уведомлений владельцу: Telegram-бот @vAssistantv2workbot (chat_id в памяти Claude) — сводка после каждого принятого пункта, чтение указаний на чекпоинтах. Правило: лимиты Codex кончились → передача сюда, коммит, стоп (не подменять Codex Claude'ом). Урок ночного автозапуска 03:00: фоновая сессия зависла на запросе разрешения на Edit — для автозапусков нужен режим с автоподтверждением правок.
- Блокеры 0.5/0.6/№10 на дедлайне — ждут владельца (домен, №3 хостер/юрисдикция, privacy policy, заявка Google; критический путь к 4.7).
- Приёмка любого диффа: дифф целиком → ruff/mypy strict (запускать `uv run mypy` без аргументов — с `.` ломается на tests/) → pytest (unit без окружения: integration должны скипаться; с окружением: всё) → gitleaks (git-mode, `MSYS_NO_PATHCONV=1`) → пуш → зелёный CI → отметка здесь → дописать docs/dev-log.md (sonnet-сабагент) → переиндексировать codebase-memory.

## Блокеры
- 0.5 (частично): BotFather (тест+бой) и OpenRouter — готово 2026-07-09, токены в локальном bootstrap.env до Infisical (1.5); проверить лимит расходов в OpenRouter. Остаются: Google Cloud + consent screen, домен, Tribute (к этапу 7).
- 0.6/№10: privacy policy публикуется после выбора домена; заявка в Google — после публикации. Дедлайн «не позже конца этапа 2» НАСТУПИЛ (2026-07-10, этап 2 закрыт): нужны действия владельца — выбрать/купить домен, решить №3 (хостер/юрисдикция влияет на privacy policy), после публикации политики — подать заявку Google (верификация — недели, критический путь к 4.7). Этап 3 можно вести параллельно, но каждый день задержки заявки сдвигает 4.7.
- Перед публичным запуском: перевыпустить OpenRouter-ключ и боевой токен бота (засветились в чате при передаче 2026-07-09).
