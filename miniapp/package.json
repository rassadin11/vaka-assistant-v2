{
  "name": "personal-assistant-miniapp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -b && vite build",
    "lint": "eslint src vite.config.ts eslint.config.js",
    "test": "vitest run --pool=forks --maxWorkers=1 --minWorkers=1",
    "typecheck": "tsc -b --pretty false"
  },
  "dependencies": {
    "preact": "^10.27.2"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.1",
    "@preact/preset-vite": "^2.10.1",
    "@types/node": "^24.0.10",
    "eslint": "^9.30.1",
    "globals": "^16.3.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.1",
    "vite": "^7.0.2",
    "vitest": "^3.2.4"
  }
}
